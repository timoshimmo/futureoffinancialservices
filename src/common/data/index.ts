
import { connectData, discoverItemsData, featuresData, productData, topCreatorData, whatIsData, speakersData, numbersData, partnerImgsData, latestData, faqData, faqFullData, speakersFullData, agendaData } from "./FFSData";

export {
  connectData, 
  discoverItemsData, 
  featuresData, 
  productData, 
  topCreatorData,
  whatIsData,
  speakersData,
  numbersData,
  partnerImgsData,
  latestData,
  faqData,
  faqFullData,
  speakersFullData,
  agendaData
};
